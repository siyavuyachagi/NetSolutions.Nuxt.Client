export interface PhysicalAddress {
  id: string;
  street: string;
  city: string;
  province: string;
  country: string;
  postalCode: string;
  createdAt: string; // or Date
  updatedAt: string; // or Date
}
